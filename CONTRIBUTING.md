# 贡献指南

## 编辑流程

1. 从 `main` 分支新建分支。
2. 修改 `source/**/*.tex` 中的正文或附录。
3. 本地运行 `powershell -ExecutionPolicy Bypass -File scripts/build.ps1`，确保 PDF 可编译。
4. 提交 PR 时说明改动章节、资料来源和是否已核对 2026 级实际情况。

## 写作原则

保留原作者署名和旧版资料来源。新增内容请尽量注明来源、日期和适用对象。

面向新生的正文优先写成可执行建议，避免只贴通知截图。对可能变化的信息使用“以当年官方通知为准”等提示，并留下待核验标记。

不要把编译产物、临时文件、个人编辑器配置提交到仓库。

## 资料更新

原始资料放在 `references/originals/`。导师名单这类表格资料可用 `scripts/bootstrap_sources.py` 重新导出 CSV，但重新生成后需要检查 diff，避免覆盖人工编辑成果。
